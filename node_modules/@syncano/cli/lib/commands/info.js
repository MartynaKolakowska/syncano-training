'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _chalk = require('chalk');

var _chalk2 = _interopRequireDefault(_chalk);

var _debug = require('../utils/debug');

var _debug2 = _interopRequireDefault(_debug);

var _printTools = require('../utils/print-tools');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

const { debug } = (0, _debug2.default)('cmd-info');

class InfoCmd {
  constructor(context) {
    debug('InfoCmd.constructor');
    this.context = context;
    this.session = context.session;
    this.Init = context.Init;
  }

  run([cmd]) {
    var _this = this;

    return _asyncToGenerator(function* () {
      (0, _printTools.echo)();
      (0, _printTools.echon)(2)(`        ${_chalk2.default.dim('username')}:`);
      (0, _printTools.echo)(` ${_chalk2.default.cyan(_this.session.userEmail)}`);

      (0, _printTools.echon)(2)(`       ${_chalk2.default.dim('full name')}:`);
      (0, _printTools.echo)(` ${_chalk2.default.cyan(_this.session.getFullName())}`);

      if (_this.session.project && _this.session.project.instance) {
        (0, _printTools.echon)(2)(`${_chalk2.default.dim('current instance')}:`);
        const fromEnv = process.env.SYNCANO_PROJECT_INSTANCE ? ' (taken from SYNCANO_PROJECT_INSTANCE environment variable)' : '';
        (0, _printTools.echo)(` ${_chalk2.default.cyan(_this.session.project.instance)}`);

        if (fromEnv) {
          (0, _printTools.echo)(19)(_chalk2.default.dim(fromEnv));
        }
      }
      (0, _printTools.echon)(2)(`         ${_chalk2.default.dim('api url')}:`);
      (0, _printTools.echo)(` ${_chalk2.default.cyan(_this.session.HOST)}`);

      const instances = yield _this.session.getInstances();
      if (instances.length < 1) {
        (0, _printTools.echo)();
        (0, _printTools.echo)(4)('You don\'t have any instances!');
        (0, _printTools.echo)();
      } else {
        (0, _printTools.echo)(2)(`   ${_chalk2.default.dim('all instances')}:`);
        instances.forEach(function (instance) {
          (0, _printTools.echo)(18)(`- ${_chalk2.default.cyan(instance.name)}`);
        });
        (0, _printTools.echo)();
      }

      if (!_this.session.project) {
        (0, _printTools.warning)(2)('No Syncano project in current folder!');
        (0, _printTools.echo)();
      }
    })();
  }
}
exports.default = InfoCmd;
module.exports = exports['default'];
//# sourceMappingURL=info.js.map